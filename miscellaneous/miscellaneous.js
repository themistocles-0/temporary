const { create } = require(__dirname+"/create.js");
const { preview } = require(__dirname+"/preview.js");
const { extract } = require(__dirname+"/extract.js");

module.exports = {
	create,
	preview,
	extract
};